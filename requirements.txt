# NOTE: PyTorch is intentionally not pinned here — the correct CUDA build must be
# chosen per-machine via the PyTorch index (see the Installation section in
# CLAUDE.md). The code requires torch>=2.3 (it uses the torch.amp.GradScaler /
# torch.amp.autocast string-device API). Validated with torch 2.4.1+cu118.

timm==0.9.12
Pillow
blobfile
mypy
numpy<2
pandas<2.2.0
pytest
requests
einops
scipy
pyhealth==1.1.4
h5py
mne==1.4.2

# Detailed evaluation metrics (labram/utils/eval_metrics.py: confusion matrix,
# ROC/PR, F1, sensitivity/specificity). scikit-learn is also a pyhealth
# dependency; pinned here because it is now imported directly.
scikit-learn
# ROC/PR curve figures for TensorBoard/ClearML (labram/utils/plots.py). Optional:
# curve logging degrades gracefully (scalar metrics still logged) if it is
# missing, but installing it enables the plotted curves.
matplotlib

# Model-architecture graph coloured by frozen/trainable layers
# (labram/utils/model_graph.py). Optional: rendering to a vector SVG needs both
# this package and the system Graphviz `dot` binary; without them the code falls
# back to a matplotlib figure.
graphviz

# Logging: labram.utils.logging imports torch.utils.tensorboard, which requires
# the `tensorboard` package to be installed.
tensorboard

# Config infrastructure (labram/configs)
PyYAML
jsonpickle
boto3
s3fs

# NOTE: the AWS SageMaker SDK needed to *submit* training jobs is deliberately
# NOT here — see requirements-sagemaker.txt. This file is also what the training
# container pip-installs, and the SDK's dependency tree (mlflow, onnx,
# tritonclient, urllib3>=2 …) conflicts with pyhealth and has no business inside
# the job.

# Experiment tracking (optional; only needed when clearml.enabled is set).
# Training degrades gracefully to TensorBoard-only logging if this is absent.
clearml

# Optional: DeepSpeed fine-tuning, only used behind the --enable_deepspeed flag
# (lazily imported in labram/runs/finetune_args.py). NOTE: the historical
# deepspeed==0.4.0 pin is INCOMPATIBLE with torch>=2.3 (it imports the removed
# torch._six) and requires `pip install --no-build-isolation`. Install a
# compatible deepspeed manually only if you need this feature.
# deepspeed
